workspace(name = "mediapipe_mlx_port")

load("@bazel_tools//tools/build_defs/repo:local.bzl", "local_repository", "new_local_repository")

# We can also add mediapipe as a local repository here to use its internals later
local_repository(
    name = "mediapipe",
    path = "./mediapipe",
)
